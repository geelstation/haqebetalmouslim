# نظام التوثيق - دليل الاستخدام

## نظرة عامة
نظام التوثيق يسمح للمسؤولين بتوثيق حسابات المستخدمين المميزين وإعطائهم صفحات بروفايل احترافية مع علامة التوثيق الزرقاء ✓.

## الميزات

### 1. توثيق الحسابات
- إضافة علامة توثيق زرقاء بجانب اسم المستخدم
- ثلاثة أحجام للعلامة: small, medium, large
- خيار إظهار نص "موثق" بجانب العلامة

### 2. صفحات البروفايل
- صفحة شخصية لكل مستخدم موثق: `/profile/:userId`
- تصميم احترافي مع:
  - صورة غلاف بتدرج بني
  - صورة بروفايل كبيرة (160x160px)
  - نبذة تعريفية
  - روابط التواصل الاجتماعي
  - عرض الأشرطة المنشورة

### 3. روابط التواصل الاجتماعي
- الموقع الشخصي 🌐
- يوتيوب 📺
- فيسبوك 📘
- تويتر 🐦

### 4. رفع الأشرطة
- المستخدمون الموثقون يمكنهم رفع أشرطتهم الخاصة
- ترتبط الأشرطة تلقائياً بصفحة البروفايل

## الاستخدام

### للمسؤولين

#### توثيق مستخدم جديد:
1. اذهب إلى Admin Panel
2. اضغط على تبويب "التوثيق"
3. اضغط على "توثيق مستخدم جديد"
4. أدخل البيانات:
   - البريد الإلكتروني (إلزامي)
   - الاسم الظاهر
   - نبذة تعريفية
   - روابط التواصل (اختياري)
5. اضغط "توثيق الحساب"

#### إلغاء التوثيق:
1. في قائمة المستخدمين الموثقين
2. اضغط "إلغاء التوثيق" للمستخدم المطلوب
3. أكد العملية

### للمستخدمين الموثقين

#### عرض صفحتك:
- رابطك الشخصي: `https://yoursite.com/profile/youremail_com`
- مثال: إذا كان بريدك `sheikh@example.com`
- الرابط: `/profile/sheikh_example_com`

#### مشاركة الصفحة:
- اضغط زر "مشاركة الصفحة" في صفحة البروفايل
- سيتم نسخ الرابط أو مشاركته مباشرة

## التطوير

### الملفات المهمة:

#### Services:
```javascript
// src/services/verificationService.js
- getVerifiedUserProfile(userId)
- updateVerifiedUserProfile(userId, data)
- verifyUser(userEmail, adminId, data)
- unverifyUser(userId)
- getAllVerifiedUsers()
- checkVerificationStatus(email)
```

#### Components:
```
src/components/
├── VerifiedBadge/
│   ├── VerifiedBadge.jsx
│   └── VerifiedBadge.css
├── VerifiedUserProfile/
│   ├── VerifiedUserProfile.jsx
│   └── VerifiedUserProfile.css
└── AdminPanel/
    ├── AdminPanel.jsx (محدّث)
    └── AdminPanel.css (محدّث)
```

### استخدام VerifiedBadge في الكود:

```jsx
import VerifiedBadge from '../VerifiedBadge/VerifiedBadge';

// صغير
<VerifiedBadge size="small" />

// متوسط مع نص
<VerifiedBadge size="medium" showText={true} />

// كبير
<VerifiedBadge size="large" />
```

### Firestore Collection Structure:

```javascript
verifiedUsers/ {
  userId: "user_email_com",
  email: "user@email.com",
  isVerified: true,
  verifiedBy: "admin_uid",
  verifiedAt: Timestamp,
  displayName: "اسم المستخدم",
  bio: "نبذة تعريفية...",
  photoURL: "https://...",
  website: "https://...",
  youtube: "https://youtube.com/@...",
  facebook: "https://facebook.com/...",
  twitter: "https://twitter.com/...",
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

## الأمان

- ⚠️ **فقط المسؤولون** يمكنهم توثيق/إلغاء توثيق الحسابات
- يتم التحقق من صلاحيات Admin في AdminPanel
- البريد الإلكتروني يُستخدم كمعرف فريد (يتم تحويل @ و . إلى _)

## تحسينات مستقبلية

- [ ] نظام طلبات التوثيق من المستخدمين
- [ ] إحصائيات للمستخدمين الموثقين
- [ ] تقييمات وتعليقات على الأشرطة
- [ ] نظام متابعة (Follow)
- [ ] إشعارات عند رفع شريط جديد
- [ ] بحث متقدم للمستخدمين الموثقين

## الدعم

للمشاكل أو الاستفسارات:
1. تحقق من console.log في المتصفح
2. تأكد من صلاحيات Firestore
3. تأكد من تفعيل Authentication في Firebase

## مثال كامل

```javascript
// فحص حالة التوثيق
import { checkVerificationStatus } from './services/verificationService';

const checkUser = async () => {
  const status = await checkVerificationStatus('user@email.com');
  if (status.isVerified) {
    console.log('مستخدم موثق!', status);
  }
};

// عرض البادج
{isVerified && <VerifiedBadge size="small" />}
```

---

**ملاحظة**: لا تنسى إضافة route للبروفايل في App.jsx:
```jsx
<Route path="/profile/:userId" element={<VerifiedUserProfile />} />
```
